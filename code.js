function mergesort(array) {
    // Get the length og the input array.
    const n = array.length;
    // Create variable for size of subarray 
    var subSize; 
    // Create variable to keep track of left subarray 
    var leftStart;
    // Start loop for subarrays 
    for (subSize =  1; subSize < n; subSize = 
    
    
    return array;
}
